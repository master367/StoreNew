# Укажите базовый образ для Go
FROM golang:1.23

# Установим рабочую директорию
WORKDIR /app

# Скопируем все файлы проекта в контейнер
COPY . .

# Установим зависимости
RUN go mod tidy

# Соберём бинарный файл
RUN go build -o main .

# Укажите порт, который будет открыт в контейнере
EXPOSE 8080

# Укажите команду для запуска
CMD ["./main"]